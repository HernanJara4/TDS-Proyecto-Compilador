// Lexico: simbolos que NO son tokens.
// Debe reportar error lexico indicando la linea de cada caracter invalido (@, #, $, ?, &).
int x;
void main() {
    x = 1 @ 2;
    y = 3 # 4;
    z = 5 $ 6;
    x = x ? y;
    x = x & 1;
}